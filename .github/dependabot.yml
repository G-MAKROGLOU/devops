version: 2
updates:
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: daily
    labels:
      - dependencies
    ignore:
      # docker/docker v28+ has a broken archive_deprecated.go; update containers first.
      - dependency-name: "github.com/docker/docker"
        update-types: ["version-update:semver-major"]
